function pasarplan(plan) {
    var tipoPlan = plan;
    if(tipoPlan="planUno"){
       document.getElementById('plan').value=tipoPlan;
}

}

function pasarplan2(plan) {
    var tipoPlan = plan;
    document.getElementById('plan').value=tipoPlan;
}
